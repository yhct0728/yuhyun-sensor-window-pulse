# Changelog

Pulse 의 주요 변경사항을 기록합니다. **타팀 보고용** 문서입니다.

- 형식: [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)
- 버전: [유의적 버전(SemVer)](https://semver.org/lang/ko/)
- "왜" 그렇게 정했는지는 각 항목에 연결된 결정 기록([ADR](docs/decisions/))을 참고하세요.

분류: **Added**(추가) · **Changed**(변경) · **Fixed**(수정) · **Removed**(제거) · **Deprecated**(폐기 예정) · **Security**(보안)

## [Unreleased]

### Changed

- **폴링 표시를 실제 동작에 맞춤 + "주기" → "기록 간격" 개명**: 헤더의 `다음 폴링 mm:ss` 카운트다운을 제거했습니다. 이 카운트다운은 0이 되어도 **아무것도 트리거하지 않는 장식**이었고(실제 수집은 별개의 60초 고정 타이머), `nodes[0]` 한 대의 주기만 보고 그려졌으며, 시(hour) 단위로 올리지 않아 1일 주기에서 `1440:58` 로 표시됐습니다. 대신 실제 사실을 표시합니다 — `1분마다 자동 확인 · N분 전 확인함`. 수집 주기는 `SYNC_INTERVAL_MS` 단일 상수로 고정해 타이머와 화면 문구가 갈라질 수 없게 했습니다. 아울러 노드의 `주기`를 **`기록 간격`**으로 개명했습니다(전송 빈도가 아니라 *장비가 값을 기록하는 간격*이며 지연·끊김 판정 기준). ([ADR-0019](docs/decisions/0019-수집주기-단일출처-기록간격-용어분리.md))

### Added

- **`지금 전송` 버튼**(노드 모니터 헤더): 자동 주기(1분)를 기다리지 않고 즉시 감시 폴더를 다시 읽어 신규 측정값을 전송합니다. 보낼 게 없으면 그 사실을 토스트로 알립니다. ([ADR-0019](docs/decisions/0019-수집주기-단일출처-기록간격-용어분리.md))

### Removed

- `usePolling` 훅 — 가짜 카운트다운 전용이라 제거하고 상대시간 갱신용 `useTick` 으로 대체했습니다.
- `theme.js` 의 `POLL_INTERVAL_SEC` — 사용처가 없는 데다 값(1시간)이 실제 수집 주기와 달랐습니다.

## [1.1.0] - 2026-06-30

> **v2 — 현장-블라인드 전면 교체 (2026-05-29).** Pulse 를 현장 모니터에서 **노드·수신 중심 수집기**로 재설계했습니다. 아래 v1 항목들은 미출시 단계의 작업으로, v2 가 대체합니다. ([ADR-0010](docs/decisions/0010-현장-블라인드-노드-수신-중심-v2.md))

### Added (v2)

- **품질 모델 — 3축(정상범위 / 고장 / 경보)**: 품질·이상을 세 가지로 분리. ① **정상범위(valid_min/max)** — 정상 센서가 가끔 뱉는 "튄 값(raw 글리치)" 1개를 컷. 노드 상세 드로어 "정상 범위" 섹션에서 센서별 raw 하한/상한 입력 → "정상범위 저장"으로 **노드 등록(`POST /nodes`)을 같은 `node_code`로 재전송**해 `valid_min`/`valid_max`(raw 기준) 갱신(sticky, WYSIWYG, 미입력=null=해제, `sensor_sync`/`name`/`sensor_count`/`spike` 미포함). **판정(범위 밖 invalid)은 백엔드 단독** — 펄스는 입력+전송만, 행 표시는 '설정됨/검사 안 함'(로컬 이상치 판정 없음). ② **고장(inactive)** — 센서 통째 고장은 `PATCH /sensors/{sensor_code} {status:"inactive"}` → 백엔드가 노드 평균·offline 감지에서 자동 제외(평균 응답 `excluded_sensors`), 복구 시 `{status:"active"}`. (레거시 `dead`/`disabled` 폐기) ③ **경보 임계(level1/2)** — 진짜 위험값 알람, 프론트(웹) 책임. ([ADR-0015](docs/decisions/0015-정상범위-재도입-3축-입력전송만.md), [ADR-0016](docs/decisions/0016-운영기준일-monitoring-from-생명주기-inactive.md))
- **개발용 자동 등록 시드(`PULSE_AUTOIMPORT`)**: 더미 .txt 로 앱을 테스트할 때, 감시 폴더의 모든 파일을 위저드와 동일 경로(`scanFolder→analyzeFile→resolveSensors→registerNode`)로 **헤드리스 일괄 등록**. 채널/종류/단위는 해석기가 추론하고, **정상범위(임계치)는 샘플 데이터에서 보수적으로 자동 도출**(`deriveRangeFromRows` — 관측 [min,max]에 넉넉한 패딩)해 채널마다 채움. **번호형 헤더(1,2,3)라 헤더로 종류를 못 잡으면 파일명에서 종류 추론**(`guessSensorTypeFromName` — 예 `03_groundwater`→groundwater)해 종류·채널 코드(GW-1)까지 채움. 형식 오류(🔴)는 GUI 게이트와 동일하게 건너뜀. 트리거 = `PULSE_AUTOIMPORT=1 npm start`(`main.js`가 렌더러에 `?test=import` 부착) → `App.jsx` 훅이 `store.autoImportFromFolder()` 호출. 운영 무관(플래그 없으면 평소 동작), 테스트 전용. 핸드오프 가이드 `docs/AUTOIMPORT_HANDOFF.md`.
- **정상범위 전체 적용(fill-down)**: 노드 상세 드로어 "정상 범위" 섹션의 각 채널 행에 **전체 적용 버튼**(`CopyPlus`) 추가. 한 채널에 하한/상한을 입력하고 누르면 **그 노드의 모든 채널에 같은 범위를 복사**(기존 값 덮어쓰기) — 채널마다 똑같은 값을 일일이 넣던 수고 제거. 로컬 상태만 갱신하므로 적용 후 검토하고 "정상범위 저장"으로 전송. 둘 다 빈 행은 버튼 비활성, 검증(하한≤상한)은 기존 입력 규칙 그대로.
- **채널별 계측 종류(sensor_type per channel)**: 한 노드에 종류가 다른 센서 혼재 허용(예: 수위 + 균열). 노드 등록(`POST /nodes`)의 `channels[]`에 채널마다 `sensor_type`/`unit`(+`valid_min/max`) 전송, 노드 레벨 `sensor_type`은 대표/폴백. 채널 코드 접두로 종류 자동 추론(WL→수위, CR→균열, GI→지중경사…), 단위는 종류 기본값. 입력 2곳 — **등록 위저드 "채널 매핑"**(채널마다 종류 Select) + **노드 상세 드로어 "센서"**(채널 행별 종류 Select, 변경 시 즉시 저장). GET 응답의 채널별 `sensor_type`도 복원. 백엔드는 같은 종류·단위끼리만 평균. ([ADR-0017](docs/decisions/0017-채널별-sensor-type-노드내-종류혼재.md))
- **운영 시작 기준일(monitoring_from)**: "이 날짜부터 진짜 운영 데이터(시운전 완료)" 노드 단위 플래그. 이전 데이터는 raw 보존하되 백엔드 분석·고장의심·기본차트에서 제외(0 떡칠된 초기 구간 건너뜀). 입력 2곳 — **등록 위저드 "확인" 단계** + **노드 상세 드로어 헤더** 날짜 입력. 전송 = 노드 등록(`POST /nodes`) body 의 `monitoring_from`(ISO/KST), 미전송=유지·null=해제, 새 날짜=리셋. 드로어는 단독 최소 본문(`nodes:set-monitoring-from`), 위저드는 등록 payload 동봉. ([ADR-0016](docs/decisions/0016-운영기준일-monitoring-from-생명주기-inactive.md))
- **수신 상태 안내 팝업**(`components/onboarding/ReceptionGuide.jsx`): 수신중/지연/끊김 기준(주기 ×1.5 / ×3, "마지막 수신=파일 저장시각")을 쉬운 한 줄 설명으로. 모니터링 탭 수신 칩 옆 **? 버튼**으로 가운데 팝업
- **파일 형식 안내 팝업**(`components/onboarding/FormatGuide.jsx`): 번호형/이름형/깊이형/값+온도형을 **쉬운 한 줄 설명 + 짧은 예시**로, 비정형(🟡)·형식 오류(🔴)도 간단히. 감지·등록 탭의 **"형식 안내" 링크** 또는 **파일 행의 형식 배지 클릭**(해당 형 강조)으로 가운데 팝업 오픈
- **시작 가이드(온보딩)**(`components/onboarding/StartGuide.jsx`): 파일 감지→노드 등록 5단계 절차 + 파일 형식 신호등 범례. 첫 실행 시 1회 자동 표시(`localStorage pulse:guideSeen`), 이후 노드 모니터 헤더 **? 버튼**으로 재호출. 감지·등록 탭 목록 헤더에도 신호등 범례(🟢 등록 가능 / 🟡 비정형 / 🔴 형식 오류) 인라인 표시

- **노드 모니터** 페이지: 모니터링 탭(메트릭 5·수신/전송 핀·테이블/카드/컴팩트 뷰·필터/정렬/검색)과 감지·등록 탭(감시 폴더·요약 3·감지 파일 목록). 우측 **노드 상세 드로어**(채널 스트림/전송 큐/진단 로그 3탭)와 **4단계 노드 등록 위저드**(감지→분석→채널 매핑→확인). 전 화면 빈 상태(제로 디폴트)
- **전송 큐** 페이지: 인제스트 연결 카드 + 요약 5 + 노드별 큐 테이블(문제 우선 정렬·재전송/전체 재전송)
- **계측기 도감** 페이지(`reference`): 읽기 전용 참조 — 탭① 계측기 목록(22종 카드, 카테고리 필터) · 탭② 공식 타입(테이블) · 탭③ 계수 사전(공식타입 서브탭 + 계수 설명). 데이터 단일 소스 `lib/sensorReference.js`. 공식·계수는 *설명용 문서*이며 Pulse 는 적용하지 않음 ([ADR-0011](docs/decisions/0011-계측기-도감-읽기전용-참조.md)). 사이드바 "채널 정의"를 이 도감으로 교체
- **파일 구조 분석 IPC** `files:analyze`(인코딩·행수·구분자·열수·주기 추정·원본 미리보기)
- 수신 상태(`live/delayed/lost`)·전송 상태(`sent/queued/retry/failed`) 표현 핀, 파일명→노드 ID 추론(`node-7.txt → YH-0007`), 채널 코드 분류(`classifyChannel`)
- **등록 위저드 채널 자동 매핑**: 헤더 열에서 코드·라벨 자동 채움 + 단위 추론(`inferUnit` — 괄호/접미사, 깊이 표식 `0.0M` 등 제외), **"모든 열 단위 일괄 적용"**. 채널 목록 스크롤·열 개수 표시
- **포맷 패턴 판정 + 엄격 등록 게이트**(`lib/formatClassify.js`): 로거 .txt 를 번호형/이름형/깊이형/값+온도형으로 추론해 감지 목록·위저드에 뱃지 표시. 표가 깨진 경우(🔴 값 열 없음 = 열 1개 이하)만 **노드 등록 차단**, 비정형(🟡)은 경고 표시 후 등록 허용(권장/느슨). 메인 스캔이 헤더+첫 데이터행을 넘겨 목록에서 바로 판정 ([ADR-0012](docs/decisions/0012-포맷-패턴-판정-엄격-등록-게이트.md))
- **깊이 프로파일(시리즈) 묶음**(`lib/series.js`): 헤더가 등간격+단위접미사(`0.0M~40.0M`)면 81개 독립 채널이 아니라 **'깊이 프로파일 1개'로 감지**해 등록 위저드/노드 상세/모니터 테이블에서 1개로 표시(개별 열 펼치기 토글). 노드 상세엔 **깊이-값 프로파일 차트**. 단위 접미사 없는 순수 번호(`1,2,3`)는 묶지 않음. 백엔드 전송은 지점별 그대로

### Added (v2)

- **배치 현장 읽기 전용 표시**: 백엔드 노드 응답의 `site_name`/`site_code`(미배치면 null)를 `normalizeBackendNode` 가 읽어, 노드 모니터 테이블 "현장" 컬럼 + 상세 드로어 헤더(📍 현장명/"미배치")에 표시. **읽기 전용**(펄스는 현장 관리·선택 안 함, 전송 페이로드에도 현장 없음 — 현장-블라인드 유지). 백엔드가 배치한 현장을 운영자가 확인하는 용도.
- **수신 주기 워터마크 버그 수정**: 신규 등록 노드의 ingest 증분 기준(`since`)이 `lastSentTs ?? lastRx`(파일 수정시각 폴백)라, 데이터가 파일 생성보다 과거(예: 5월 데이터를 오늘 등록)면 전부 제외돼 **0건 전송**되던 문제 수정 → `lastSentTs ?? 0`(신규=전체 백필). 실측: 5월~6월 지중경사계 2,673건(33일×81깊이) 백필 → 201, `last_seen` 갱신 확인.
- **노드 상세에서 수신 주기 수정 → 백엔드 저장(완전 영속)**: 노드 드로어 메타의 "수신 주기"를 인라인 Select 로 변경(`updateNodeInterval`). 변경 시 로컬 즉시 반영(수신 판정 재계산) + **`PATCH /api/pulse/v1/nodes/{code}` `{report_interval_min}`** 전용 API 호출(2026-06-01 백엔드 신설, 센서/채널 무영향). 등록 위저드는 register 본문에 `report_interval_min` 동봉. GET 의 `report_interval_min` 을 `normalizeBackendNode` 가 읽어 재시작 후 복원 → **주기 변경이 재시작에도 유지**. 실측: PATCH 30분→200, GET 30 저장 확인. (`nodes:set-interval` IPC / `setNodeIntervalBackend`)
- **수신 주기 선택지 확장 + 기본 1일**: 10·30분/1·2·3·6·12시간/1·2·7일. 기본값 60분→**1440분(1일)**(통상 디폴트), 파일에서 주기 감지 시 그 값 우선.
- **`pore_pressure`(간극수압계) 종류 분리 + groundwater 정정**: 백엔드가 groundwater(지하수위계·m)와 pore_pressure(간극수압계·kPa)를 분리 → 펄스 `SENSOR_TYPES` 에 `pore_pressure`(약어 PWP) 추가, groundwater 채널약어 **WL→GW**(water_level 과 충돌 해소). `guessSensorType` 정정: `간극수압→pore_pressure`(예전 groundwater 오매핑 수정 — 80053 이 간극수압으로 뜨던 혼란의 근원), `지하수위→groundwater`, `수위→water_level`. register `unit` 전송 배관 추가(노드 `unit`·`channels[].unit`, 있을 때만 — 평소엔 백엔드 카탈로그 폴백).
- **ingest 센서코드 = 백엔드 정본 사용(재연결)**: 재시작 후 백엔드 노드를 파일에 재연결할 때, 센서코드를 타입 약어로 재계산하지 않고 **백엔드가 가진 실제 `sensor_code` 를 그대로 사용**(개수 일치 시 파일-도출 구조에 위치 매핑). 타입/약어가 바뀌어도 ingest 가 기존 센서와 항상 일치(개수 불일치 시 재계산 유지 → 다음 등록 replace 가 정합). 실측: 80053 3채널 × 385건 ingest → 201.
- **등록 후 형식 깨짐 감지 → "형식 오류" 상태**: 폴링(60초)마다 원본 .txt 를 다시 읽을 때 등록 때와 동일한 `classifyFormat` 으로 **형식 재검증**. 표가 아니게 되거나(열 1개 등) **데이터 줄 첫 칸이 더 이상 시각이 아니면**(쉼표 낙서 포함) 노드를 **빨강 "형식 오류"** 로 표시하고 전송 중단(원본 없음 tombstone 과 별개). 형식이 정상으로 돌아오면 자동 재개. 표시: 테이블/카드/컴팩트 배지(`FormatErrorBadge`)·드로어 배너·모니터 집계에서 제외(수신중 오인 방지). `readMeasurements` 가 원본 첫 두 줄(head/sample) 반환, `isDateLike` export.
- **"열 → 센서" 구조 해석기**(`lib/sensorModel.js` `resolveSensors`): 값 열 개수 ≠ 센서 개수라는 원칙으로 패턴마다 센서 수·코드를 도출하는 **단일 진실원**(등록 채널 + 추후 ingest 열 매핑 공통). 규칙 — 번호형/이름형 = 열마다 1센서(N), **깊이형 = 전체 1센서**(깊이는 ingest `depthLabel`), **값+온도형 = 온도는 부속(센서 아님)**. 등록 위저드가 이 결과만 사용(센서 수·코드·미리보기). 실파일 검증: 80053→3, 수위계_All→2, W-3→1, 지중경사계→1.
- **온전성(integrity) 경고**(비차단): 샘플이 전부 0인 **죽은 열**, 이름은 A·B 2축인데 깊이 프로파일이 1세트뿐(**B축 누락 가능**) 등을 위저드 분석·매핑·확인 단계에 표시(권장/느슨 게이트 유지 — error 만 차단). 번호형(`1,2,3`) 헤더도 헤더로 인식하도록 `looksHeader` 보강.
- **깊이형 단일 센서 코드 편집** — 깊이형은 데이터상 A/B 구분이 없어 추측하지 않고 센서 1개로 등록하되, 코드를 위저드에서 편집(기본값 인클리노미터=`A` 주축 관례, 그 외 `P`). 백엔드 센서코드 미리보기 `{node}-{code}` 표시.

### Changed (v2)

- **채널 코드 접두 약어가 "그 채널의 sensor_type"를 따라감** (90030 GI→GW 근본수정): 이전엔 채널 코드를 **노드 대표 종류** 하나로 접두 생성 → 한 노드에 종류가 섞이면 코드 약어와 종류가 어긋남(예: 지하수위 채널이 노드 대표 지중경사계에 휩쓸려 `90030-GI-1`). 백엔드 ingest 는 `sensorCode` 로만 센서를 찾으므로 코드↔종류 불일치 시 매칭 실패→데이터 유실 위험. 이제 `resolveSensors` 가 채널별 종류(명시 override > 헤더 추론 > 노드 폴백)로 코드 접두를 뽑고 **접미 번호(위치/심도)는 보존** → 채널 종류를 바꾸면 코드가 즉시 따라감(`GI-1 → GW-1`). 등록·측정이 같은 `sensors[].code` 단일 소스를 공유해 약어↔종류 항상 일치. 약어표(14종)는 백엔드가 펄스 기존 약어를 표준 채택 → 표 자체는 수정 없음. ⚠️ 기존 `90030` 의 `GI-1→GW-1` 전환은 백엔드 리네임과 **동시 배포로 조율**(단독 선반영 금지). ([ADR-0018](docs/decisions/0018-채널코드-접두가-채널종류를-따라감.md))
- **깊이형 등록이 채널 1개를 명시 전송** — 이전엔 `channels` 미전송 → 백엔드 sensor_type 템플릿이 적용돼 인클리노미터가 **A·B 2센서**로 생성되던 문제를 수정. 이제 해석기가 깊이형=센서 1개로 보고 채널 1개를 보내 정확히 1개 생성.
- **`sensor_sync:"replace"` 전송 — 잉여 센서 자가 정리** ✅ 양측 완료(2026-05-31): 펄스는 파일 패턴으로 센서 집합을 단언하므로 register 본문에 `sensor_sync:"replace"` 를 함께 보냄 → 백엔드가 `channels` 에 없는 기존 센서를 정리(데이터 없으면 삭제, 있으면 `is_active=false` 비활성). 안전장치: `channels` 가 있을 때만 동작(빈 channels=대량삭제 방지)이라 `nodeRegisterBody` 도 채널 없으면 필드 미전송. 이로써 깊이형 오등록(A·B 2개)이나 열 감소가 재등록만으로 자동 정합. 실측: `지중경사계_(I-4-AB)` 정리 후 센서 1개(-A).
- **노드 등록 시 채널(파일 열)별 센서 생성** ✅ 양측 완료(2026-05-31): `nodeRegisterBody` 가 `channels:[{ch,code}]`(+참고용 `sensor_count`) 전송 → 백엔드가 **채널당 센서 1개** 생성(`{node_code}-{code}` 명명). channels 없으면 종류 템플릿(시리즈/깊이형). 멱등 additive upsert(재전송 시 빠진 채널만 추가, 감소 시 유지). **실측 검증**: 80053 channels[WL-1/2/3] → 센서 3개 `80053-WL-1/2/3`. `normalizeBackendNode` 가 GET 응답 `sensors[].sensor_code` 를 채널 코드로 읽음
- **ingest sensorCode 규칙 확정** (`ingestSensorCode`): register 는 **bare code**(`WL-1`)를, ingest 는 **`{node_code}-{code}`**(`80053-WL-1`)를 보내야 백엔드 센서와 매칭됨(백엔드 명명 규칙). 헬퍼가 prefix 부여 + 이미 prefix 된 코드는 중복 안 붙임(GET 으로 받은 sensor_code 그대로 써도 안전). `ingestBody({nodeId, channelCode, …})` 가 자동 조합. ⚠️ ingest 전송 자체는 아직 미구현(Phase 4)
- **node_code = 파일명 그대로**(`inferNodeId` 재작성): 임의 `YH-` prefix·숫자추출 폐지 → 확장자만 뗀 파일명을 노드 식별키로(80053, W-3, 수위계_(All)…). 정보 손실·충돌(예전 W-3→YH-0003) 제거. 특수문자/한글은 URL 사용 시 `encodeURIComponent` 처리. ⚠️ 옛 규칙으로 등록된 백엔드 노드(YH-####)는 더 이상 매칭 안 되므로 재등록 필요
- **백엔드 노드 조회(GET) 연동 + "자기 노드만" 필터**: `GET /api/pulse/v1/nodes`(목록)·`/{node_code}`(단건)을 메인 `nodes:list` IPC 로 호출, 시작/새로고침 시 모니터를 채움. 백엔드는 전체를 주지만(펄스별 링크 없음) **이 수집기의 감시폴더 파일 node_code 와 매칭되는 노드만 표시**(남의 PC 노드 제외). 백엔드 응답의 `sensors[].sensor_code`를 채널 코드로 매핑(ingest sensorCode 호환) — 이전엔 센서 이름이 들어가던 버그 수정
- **원본 없음(tombstone) 감지·표시**: 다시 스캔 시 등록 노드의 `sourceFile`이 사라졌으면(삭제/이름변경) **"원본 없음"** 으로 표시(회색·수집 제외), 끊김(수신 지연)과 분리. 폴더 오프라인이면 오탐 방지로 판정 보류. 목록/드로어 안내 + 재등록·삭제로 정리. 이름변경=옛 노드 tombstone + 새 파일 신규 등록(연속성은 웹에서 재바인딩)
- **백엔드 API 계약 모듈**(`lib/backendApi.js`): 새 펄스 트랙 `/api/pulse/v1/*`(heartbeat·nodes·status·ingest) 경로·`X-API-Key` 인증·요청 본문·클라이언트를 단일 소스로 정의(레거시 `/api/legacy/*`는 어디에도 두지 않음)
- **노드 등록 백엔드 실전송 연결**: 설정 "백엔드 연결" 카드(URL·API키, **키는 메인 프로세스에만 저장**) + 메인 `nodes:register` IPC → `POST /api/pulse/v1/nodes`. 등록 시 메모리 추가 + 백엔드 전송, 결과 토스트(미설정/401/실패 구분). 미설정·계측기 종류 미지정·비-Electron 이면 메모리만. heartbeat/ingest 전송은 이후
- 노드 등록의 `name` 은 **node_code(노드 ID) 그대로 전송**(가칭 입력 폐지) — 백엔드 표시 라벨 `#{id} {name}` 소스
- **노드 등록에 "계측기 종류"(선택) 추가** — 백엔드 nodes 등록 enum(13종, 값=코드/라벨=한글)에서 고르고 노드 메타로 저장(파일명에 종류가 있으면 자동 추천: 지중경사계→inclinometer 등). 노드 상세 헤더·목록·확인 단계에 표시. **전송 페이로드엔 미포함**(노드 등록 정보용)
- **등록 위저드 채널 매핑 단순화** — 라벨·단위 입력 제거(원본 전달 원칙: 값의 의미·단위는 백엔드 몫). 매핑 단계는 **노드 ID + 수신 주기**만 받고, 채널은 헤더 코드를 그대로 사용(읽기 전용 미리보기). 깊이형은 프로파일로 묶어 표시. 전송 페이로드는 그대로 `{ nodeId, channel, ts, value }`
- 노드 모니터 **감지·등록 탭을 디자인 프로토타입(pulse2-views)에 맞춤**: 감시 폴더 카드("감시 중" 핑 + 아이콘형 다시 스캔), 요약 3카드(값 26px·미등록 앰버), 감지 파일 카드(헤더 바 + h-14 행, 미등록 행 앰버 배경·"미등록" 뱃지+검은 등록 버튼, 등록 행 "모니터링 중" 핑), 하단 안내. 본문 패딩 px-8 py-6
- 전송 페이로드를 `{ nodeId, channel, ts, value }` 로 고정 — **현장 필드 없음**(현장 stamp 는 백엔드)
- 사이드바를 5개 메뉴(노드 모니터/전송 큐/채널 정의/진단 로그/설정) + "현장-블라인드" 안내로 교체

### Removed (v2)

- **펄스 로컬 이상치 판정·표시 제거** — ADR-0013 으로 잠깐 넣었던 `lib/outlier.js`(범위 밖 판정)·드로어 행 '이상치/정상' 표식·노드 목록 이상치 요약 배지(`NodeOutlierBadge`)를 삭제. 정상범위 **입력·전송 자체는 유지**되며(위 Added 3축 ①), 범위 밖 invalid 판정은 백엔드 단독이 됨(판정=백엔드 원칙). ([ADR-0015](docs/decisions/0015-정상범위-재도입-3축-입력전송만.md))
- **진단 로그 페이지·검색(⌘K) 제거** — 미사용 스텁이라 사이드바에서 삭제(메뉴 4개: 노드 모니터/전송 큐/계측기 도감/설정). 노드 상세 드로어의 "진단 로그" 탭은 유지
- **현장(Site) 개념 전면 제거** — 현장 등록/선택/매핑 UI, `siteCode`, 정본 현장 카탈로그
- **안전 판정(정상/의심/오류) 제거** — `anomalyDetection`, `STATUS_LABEL`, 안전판정 Badge (GeoMonitor 책임)
- v1 화면/모듈 삭제: Dashboard·Settings·NodeDetail·SensorTypes 페이지, `sensor-types`/`sensor-reference`/`dashboard`/`node-detail`/`settings` 컴포넌트, `mockData`·`mockNodeData`·`anomalyDetection`·`sensorTypes`·`permissions` 등

### Added



- 노드 상세 로그 데이터 테이블에 **컬럼별 정렬 버튼**(시각·센서값·상태)과 **표시 행 수 선택**(10/25/50/100, 기본 25행) 추가, 기본 정렬을 **최신순**(시각 내림차순)으로
- **계측기 종류 관리**: 사이드바 "계측기 종류" 페이지(22종 시드·5개 카테고리 필터·카드/상세 패널, [기본 정보]·[사용 센서] 탭). 마스터 메타(이름/코드/단위/카테고리/위험방향)만 관리하며, 편집은 `permissions` 한 곳 토글로 **read-only 전환 가능**(웹 SSOT 대비). ([ADR-0008](docs/decisions/0008-계측기-종류-마스터-종류공식-센서보정.md), [ADR-0009](docs/decisions/0009-pulse-원본전달-공식보정-제거.md))
- **수신 신선도 모니터링**: 노드별 정상/지연/끊김 판정(원본 .txt mtime + 예상 주기), 노드 행·현장 카드 배지, 헤더 미수신/지연 경보, 새로고침 시 재수신 반영. 메트릭 카드를 값 → 수신 상태로 재해석 ([ADR-0005](docs/decisions/0005-수신-신선도-모니터링.md))
- 아키텍처 문서 체계 도입: 결정 기록(ADR) + C4 다이어그램(LikeC4·Mermaid) + 본 Changelog ([ADR-0001](docs/decisions/0001-adr-c4-changelog-도입.md))
- 감지된 파일 목록에서 **행 전체를 클릭**하면 선택이 토글되도록 클릭 영역 확대 ([ADR-0002](docs/decisions/0002-감지파일-행-전체-클릭-토글.md))
- 파일 1개 이상 선택 시 화면 하단에 **일괄 등록 플로팅 토스트 바** 표시, 클릭하면 일괄 등록 모달 ([ADR-0003](docs/decisions/0003-일괄등록-플로팅-토스트.md))

### Changed

- **Pulse 는 메모장 원본 데이터를 가공 없이 전달** — 노드 상세에서 적용값/원본값 토글·"보정" 버튼 제거(원본값만 표시). 값 변환·보정(공식/offset/scale)은 Pulse 책임이 아니라 백엔드/웹이 담당. 계측기 종류는 마스터만 유지(공식 설정 탭 제거). ([ADR-0009](docs/decisions/0009-pulse-원본전달-공식보정-제거.md))
- 대시보드 "모니터링 중" 기본 뷰를 카드 → **컴팩트**로 변경하고, 뷰 토글에서 컴팩트를 맨 앞에 배치 ([ADR-0009](docs/decisions/0009-pulse-원본전달-공식보정-제거.md))
- 감지된 파일의 "일괄 등록 액션 바"를 리스트 상단 인라인 → **화면 하단 플로팅 토스트** 위치로 이동 ([ADR-0003](docs/decisions/0003-일괄등록-플로팅-토스트.md))
- 노드 등록 시 현장 입력을 **즉석 생성 → 정본 목록 선택**으로 전환하고, 현장 연결 키를 이름 → **`site_code`** 로 교체 ([ADR-0004](docs/decisions/0004-현장은-정본-선택-site-code-키.md))

### Fixed

- 페이지 스크롤 시 왼쪽 사이드바가 함께 밀려 잘리던 문제 — 사이드바를 화면에 **고정(sticky)** 처리

---

> 릴리스를 끊을 때: 위 `[Unreleased]` 항목을 `## [x.y.z] - YYYY-MM-DD` 섹션으로 옮기고, 새 `[Unreleased]` 를 비워 둡니다.
